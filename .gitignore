# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Scraped raw HTML cache (regenerable; kept locally as the parser test corpus)
data/cache/

# OS / editor cruft
.DS_Store

# Local Claude Code settings (per-machine permissions, not shared)
.claude/settings.local.json
